---
import "@fontsource/ibm-plex-sans/400.css";
import "@fontsource/ibm-plex-sans/500.css";
import "@fontsource/ibm-plex-sans/600.css";
import "@fontsource/instrument-serif/400.css";
import { company } from "../data/site";
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  splitAxis?: boolean;
}

const {
  title = "IT Art Studio — Conseil technique et direction créative",
  description = "IT Art Studio est un studio indépendant de conseil et de production : architecture cloud, observabilité, IA appliquée, direction visuelle, films, images 3D et collections.",
  image = "/assets/hero-it.jpg",
  splitAxis = false
} = Astro.props;

const siteUrl = "https://itart.studio";
const canonical = new URL(Astro.url.pathname, siteUrl);
const socialImage = new URL(image, siteUrl);
const base = import.meta.env.BASE_URL;
const homeHref = base.endsWith("/") ? base : `${base}/`;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="theme-color" content="#090b10" />
    <link rel="canonical" href={canonical} />
    <link
      rel="icon"
      href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Crect width='64' height='64' rx='8' fill='%23090b10'/%3E%3Cpath d='M14 17h7v30h-7zm12 0h24v7H26zm8 12h16v7H34zm-8 11h24v7H26z' fill='%23f1eee6'/%3E%3C/svg%3E"
    />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="fr_FR" />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={socialImage} />
    <meta name="twitter:card" content="summary_large_image" />
  </head>
  <body class:list={{ "has-split-axis": splitAxis }}>
    <a class="skip-link" href="#contenu">Aller au contenu</a>

    <slot />

    <footer class="site-footer">
      <a class="brand brand--footer" href={homeHref} aria-label="IT Art Studio — accueil">
        <span class="brand__it">IT</span>
        <strong>STUDIO</strong>
        <span class="brand__art">ART</span>
      </a>
      <p class="site-footer__legal">{company.legalForm} au capital de {company.capital} · RCS {company.rcs}</p>
      <div class="site-footer__contact">
        <a href={`mailto:${company.email}`}>{company.email}</a>
        <a href={`${homeHref}mentions-legales/`}>Mentions légales</a>
      </div>
    </footer>
  </body>
</html>
