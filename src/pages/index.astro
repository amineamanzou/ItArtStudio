---
import { company, methodSteps, services } from "../data/site";
import BaseLayout from "../layouts/BaseLayout.astro";

const itServices = services.filter((service) => service.practice === "IT");
const artServices = services.filter((service) => service.practice === "ART");
const assetBase = import.meta.env.BASE_URL.endsWith("/")
  ? `${import.meta.env.BASE_URL}assets`
  : `${import.meta.env.BASE_URL}/assets`;
---

<BaseLayout>
  <main id="contenu">
    <section class="hero" aria-labelledby="hero-title">
      <article class="hero-practice hero-practice--it">
        <picture class="hero-practice__media">
          <source srcset={`${assetBase}/hero-it.avif`} type="image/avif" />
          <source srcset={`${assetBase}/hero-it.webp`} type="image/webp" />
          <img src={`${assetBase}/hero-it.jpg`} alt="Architecture technique sombre traversée de signaux cyan" width="1920" height="1080" />
        </picture>
        <div class="hero-practice__copy">
          <p>IT</p>
          <h2>Comprendre le système. Construire ce qui doit fonctionner.</h2>
          <a href="#expertises-it">Voir la pratique IT</a>
        </div>
      </article>

      <article class="hero-practice hero-practice--art">
        <picture class="hero-practice__media">
          <source srcset={`${assetBase}/hero-art.avif`} type="image/avif" />
          <source srcset={`${assetBase}/hero-art.webp`} type="image/webp" />
          <img src={`${assetBase}/hero-art.jpg`} alt="Atelier sombre composé de matière, de volume et d'outils cuivrés" width="1920" height="1080" />
        </picture>
        <div class="hero-practice__copy">
          <p>ART</p>
          <h2>Définir un langage visuel. Produire les images, les films et les objets.</h2>
          <a href="#expertises-art">Voir la pratique ART</a>
        </div>
      </article>

      <div class="hero-core">
        <h1 id="hero-title"><span>IT</span> <strong>STUDIO</strong> <span>ART</span></h1>
        <p>Conseil technique. Direction créative. Production.</p>
      </div>
    </section>

    <section class="activity-section" aria-labelledby="activity-title">
      <p class="activity-section__lead">IT + ART · un même studio</p>
      <div>
        <h2 id="activity-title">Conseil technique d'un côté. Direction et production visuelles de l'autre.</h2>
        <p>
          IT Art Studio est un studio indépendant de conseil et de production. La pratique IT
          intervient sur l'architecture cloud, l'observabilité, l'IA appliquée et la formation. La
          pratique ART conçoit des directions visuelles, des images, des films et des animations 3D,
          ainsi que des pièces et des collections capsules.
        </p>
      </div>
    </section>

    <section class="services-section" id="expertises" aria-labelledby="services-title">
      <header class="section-heading">
        <h2 id="services-title">Chaque mission se termine par quelque chose d'utilisable.</h2>
        <p>Le livrable peut être une architecture, un prototype, une équipe formée, une image ou une collection.</p>
      </header>

      <div class="practice-services practice-services--it" id="expertises-it">
        <header>
          <span>Pratique IT</span>
          <p>Architecture · observabilité · cloud · IA</p>
        </header>
        <div class="service-list">
          {itServices.map((service, index) => (
            <article class="service-item">
              <span>{String(index + 1).padStart(2, "0")}</span>
              <h3>{service.title}</h3>
              <p>{service.description}</p>
            </article>
          ))}
        </div>
      </div>

      <div class="practice-services practice-services--art" id="expertises-art">
        <header>
          <span>Pratique ART</span>
          <p>Direction visuelle · 3D · contenus · collections</p>
        </header>
        <div class="service-list">
          {artServices.map((service, index) => (
            <article class="service-item">
              <span>{String(index + 5).padStart(2, "0")}</span>
              <h3>{service.title}</h3>
              <p>{service.description}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="method-section" id="approche" aria-labelledby="method-title">
      <header>
        <h2 id="method-title">Un déroulé lisible, quel que soit le projet.</h2>
      </header>
      <ol>
        {methodSteps.map((step) => (
          <li>
            <h3>{step.title}</h3>
            <p>{step.description}</p>
          </li>
        ))}
      </ol>
    </section>

    <section class="contact-section" id="contact" aria-labelledby="contact-title">
      <p>Un système à fiabiliser, un prototype à tester ou une direction visuelle à produire ?</p>
      <h2 id="contact-title">Écrire au studio.</h2>
      <p>Quelques lignes suffisent : le contexte, l'échéance et le livrable attendu.</p>
      <a href={`mailto:${company.email}`}>{company.email}</a>
    </section>
  </main>
</BaseLayout>
